import React from 'react';

interface VoiceflowBotProps {
  fullPage?: boolean;
}

const VoiceflowBot: React.FC<VoiceflowBotProps> = () => {
  // Component is now just a placeholder since the AI Assistant is loaded directly in index.html
  return null;
};

export default VoiceflowBot;